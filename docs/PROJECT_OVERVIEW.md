# Project Overview: Visual Guide

## 🎯 Project Purpose

```
┌─────────────────────────────────────────────────────────────┐
│                                                               │
│  Compare MLflow vs W&B for ML Experiment Tracking            │
│                                                               │
│  Using: Chest X-Ray Classification (COVID-19 Detection)      │
│                                                               │
└─────────────────────────────────────────────────────────────┘
```

---

## 🔄 Workflow Diagram

```
┌──────────────┐
│   Dataset    │  COVID-19 Image Dataset (3 classes)
└──────┬───────┘
       │
       ▼
┌──────────────────┐
│  Data Loading    │  src/data/data_loader.py
│  & Processing    │  • Train/Val/Test split
└────────┬─────────┘  • Image preprocessing
         │
         ▼
    ┌────────┐
    │ Model  │  src/models/cnn_model.py
    │  CNN   │  • Custom architecture
    └───┬────┘  • 4 conv layers
        │       • Dropout regularization
        │
        ├─────────────────┬─────────────────┐
        │                 │                 │
        ▼                 ▼                 ▼
  ┌──────────┐      ┌──────────┐     ┌──────────┐
  │  MLflow  │      │   W&B    │     │  Compare │
  │ Tracking │      │ Tracking │     │   Both   │
  └────┬─────┘      └────┬─────┘     └────┬─────┘
       │                 │                 │
       ▼                 ▼                 ▼
  ┌──────────┐      ┌──────────┐     ┌──────────┐
  │ MLflow   │      │   W&B    │     │   Side-  │
  │    UI    │      │ Dashboard│     │   by-    │
  │ (Local)  │      │  (Cloud) │     │   Side   │
  └──────────┘      └──────────┘     └──────────┘
```

---

## 📁 Project Architecture

```
┌─────────────────────────────────────────────────────────────┐
│                     PROJECT ROOT                              │
├─────────────────────────────────────────────────────────────┤
│                                                               │
│  📄 Entry Points                                             │
│  ├── README.md .................... Main documentation       │
│  ├── GETTING_STARTED.md ........... Quick reference          │
│  ├── BEFORE_AFTER.md .............. Transformation guide     │
│  └── Makefile ..................... Command shortcuts        │
│                                                               │
│  📦 Source Code (src/)                                       │
│  ├── models/ ...................... CNN architecture         │
│  ├── data/ ........................ Data loaders             │
│  ├── tracking/ .................... MLflow & W&B            │
│  └── utils/ ....................... Helper functions         │
│                                                               │
│  🚀 Execution Scripts (scripts/)                             │
│  ├── train_mlflow.py .............. Train with MLflow       │
│  ├── train_wandb.py ............... Train with W&B          │
│  ├── compare_mlflow_wandb.py ...... Compare both            │
│  ├── run_hyperparameter_tuning.py  MLflow tuning            │
│  └── run_wandb_hyperparameter_tuning.py  W&B tuning         │
│                                                               │
│  ⚙️  Configuration (configs/)                                │
│  ├── mlflow/ ...................... MLflow configs           │
│  │   ├── experiments.yaml                                    │
│  │   ├── hyperparameters.yaml                                │
│  │   └── quick_test.yaml                                     │
│  └── wandb/ ....................... W&B configs              │
│      └── hyperparameters.yaml                                │
│                                                               │
│  📚 Documentation (docs/)                                    │
│  ├── README.md .................... Documentation index      │
│  ├── guides/ ...................... Detailed guides          │
│  ├── mlflow/ ...................... MLflow docs              │
│  ├── wandb/ ....................... W&B docs                 │
│  └── examples/ .................... Code examples            │
│                                                               │
│  💾 Data & Outputs (gitignored)                             │
│  ├── Covid19-dataset/ ............. Image dataset           │
│  ├── mlruns/ ...................... MLflow experiments       │
│  └── wandb/ ....................... W&B experiments          │
│                                                               │
└─────────────────────────────────────────────────────────────┘
```

---

## 🔧 Component Interaction

```
┌─────────────────────────────────────────────────────────────┐
│                   USER INTERFACE                              │
└───────┬─────────────────────────────────────────────────────┘
        │
        │  Commands (via Makefile)
        │
        ▼
┌───────────────────────────────────────────────────────────┐
│                  EXECUTION LAYER                            │
│  (scripts/)                                                 │
│                                                             │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐       │
│  │   Training  │  │   Tuning    │  │  Comparison │       │
│  │   Scripts   │  │   Scripts   │  │   Scripts   │       │
│  └──────┬──────┘  └──────┬──────┘  └──────┬──────┘       │
└─────────┼────────────────┼────────────────┼──────────────┘
          │                │                │
          └────────────────┼────────────────┘
                           │
                           ▼
┌───────────────────────────────────────────────────────────┐
│                    CORE LOGIC LAYER                         │
│  (src/)                                                     │
│                                                             │
│  ┌──────────┐   ┌──────────┐   ┌──────────────────┐      │
│  │  Models  │◄──┤   Data   │◄──┤   Tracking       │      │
│  │          │   │  Loaders │   │  (MLflow / W&B)  │      │
│  └──────────┘   └──────────┘   └──────────────────┘      │
│                                                             │
└─────────────────────────┬───────────────────────────────────┘
                          │
                          ▼
┌───────────────────────────────────────────────────────────┐
│                  CONFIGURATION LAYER                        │
│  (configs/)                                                 │
│                                                             │
│  Hyperparameters, Training Settings, Experiment Configs    │
└───────────────────────────────────────────────────────────┘
```

---

## 🎨 Model Architecture

```
Input Image (128x128x3)
       │
       ▼
┌─────────────────┐
│   Conv2D (16)   │  3x3 kernel
│   ReLU          │
│   MaxPool2D     │  2x2
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│   Conv2D (64)   │  3x3 kernel, padding='same'
│   ReLU          │
│   MaxPool2D     │  2x2
│   Dropout(0.25) │
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│  Conv2D (128)   │  3x3 kernel, padding='same'
│   ReLU          │
│   MaxPool2D     │  2x2
│   Dropout(0.3)  │
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│  Conv2D (128)   │  3x3 kernel, padding='same'
│   ReLU          │
│   MaxPool2D     │  2x2
│   Dropout(0.4)  │
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│    Flatten      │
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│  Dense (128)    │  ReLU
│  Dropout(0.25)  │
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│   Dense (64)    │  ReLU
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│   Dense (3)     │  Softmax
└────────┬────────┘
         │
         ▼
Output: [COVID-19, Viral Pneumonia, Normal]
```

---

## 🔀 Experiment Tracking Flow

### MLflow Tracking

```
┌──────────────┐
│ Start Train  │
└──────┬───────┘
       │
       ▼
┌─────────────────┐
│ mlflow.start_run│
└──────┬──────────┘
       │
       ├─► Log parameters (lr, batch_size, epochs)
       ├─► Log metrics (loss, accuracy) per epoch
       ├─► Log artifacts (model, confusion matrix)
       │
       ▼
┌─────────────────┐
│  mlflow.end_run │
└──────┬──────────┘
       │
       ▼
┌─────────────────┐
│   MLflow UI     │  http://localhost:5000
│  (View Results) │
└─────────────────┘
```

### W&B Tracking

```
┌──────────────┐
│ Start Train  │
└──────┬───────┘
       │
       ▼
┌─────────────────┐
│  wandb.init()   │
└──────┬──────────┘
       │
       ├─► Log config (hyperparameters)
       ├─► Log metrics (real-time) per batch/epoch
       ├─► Log artifacts (model, plots, confusion matrix)
       ├─► Log system metrics (GPU, CPU usage)
       │
       ▼
┌─────────────────┐
│ wandb.finish()  │
└──────┬──────────┘
       │
       ▼
┌─────────────────┐
│ W&B Dashboard   │  https://wandb.ai
│ (View Results)  │  Cloud-based, real-time
└─────────────────┘
```

---

## 📊 Data Flow

```
Kaggle Dataset
      │
      │ download
      ▼
Covid19-dataset/
├── COVID-19/
├── Viral Pneumonia/
└── Normal/
      │
      │ load & split
      ▼
┌─────────────────────────────────┐
│  Train (80%)                    │
│  Val (10%)                      │
│  Test (10%)                     │
└─────────┬───────────────────────┘
          │
          │ batch & preprocess
          ▼
┌─────────────────────────────────┐
│  DataLoader                     │
│  • Resize to 128x128            │
│  • Normalize                    │
│  • Batch (default: 32)          │
└─────────┬───────────────────────┘
          │
          │ feed to model
          ▼
┌─────────────────────────────────┐
│  Training Loop                  │
│  • Forward pass                 │
│  • Calculate loss               │
│  • Backward pass                │
│  • Update weights               │
│  • Log to MLflow/W&B            │
└─────────┬───────────────────────┘
          │
          │ evaluate
          ▼
┌─────────────────────────────────┐
│  Validation/Test                │
│  • Accuracy                     │
│  • Precision/Recall/F1          │
│  • Confusion Matrix             │
│  • Per-class Metrics            │
└─────────────────────────────────┘
```

---

## 🚀 User Journey Map

### First-Time User

```
1. Clone Repo
   │
   ▼
2. Read README.md (main overview)
   │
   ▼
3. Read GETTING_STARTED.md (commands)
   │
   ▼
4. Run: make install
   │
   ▼
5. Run: make download
   │
   ▼
6. Run: make wandb-quick (first experiment!)
   │
   ▼
7. View results in W&B dashboard
   │
   ▼
8. Run: make mlflow-quick
   │
   ▼
9. Run: make mlflow-ui
   │
   ▼
10. Run: make compare (see differences)
    │
    ▼
11. Explore hyperparameter tuning
```

### Advanced User

```
1. Edit configs/wandb/hyperparameters.yaml
   │
   ▼
2. Run: make wandb-tune
   │
   ▼
3. Analyze results in W&B dashboard
   │
   ▼
4. Modify model architecture in src/models/cnn_model.py
   │
   ▼
5. Run custom training: make train-custom EPOCHS=100
   │
   ▼
6. Compare with MLflow: make full-test
   │
   ▼
7. Read detailed guides in docs/guides/
```

---

## 🎯 Key Features by Component

### Makefile (Abstraction Layer)
- ✅ Hides complexity
- ✅ Short, memorable commands
- ✅ Self-documenting (`make help`)
- ✅ Variable support for customization

### Configuration Files
- ✅ YAML-based (human-readable)
- ✅ Separate configs per tool
- ✅ Parameter grid support
- ✅ Quick test options

### Source Code
- ✅ Modular design
- ✅ Clear separation of concerns
- ✅ Tracker abstraction (MLflow/W&B)
- ✅ Reusable components

### Documentation
- ✅ Progressive disclosure
- ✅ Multiple entry points
- ✅ Visual diagrams
- ✅ Before/After comparisons

---

## 📈 Comparison: MLflow vs W&B

```
Feature Comparison
─────────────────────────────────────────────────────────
                    │  MLflow  │   W&B    │
─────────────────────────────────────────────────────────
Local First         │    ✓     │    ✗     │
Cloud-Based         │    ✗     │    ✓     │
Real-Time Updates   │    ✗     │    ✓     │
Advanced Viz        │    ✗     │    ✓     │
Model Registry      │    ✓     │    ✓     │
Collaboration       │    ✗     │    ✓     │
Free Tier           │    ✓     │    ✓     │
Self-Hostable       │    ✓     │  Limited │
Learning Curve      │   Easy   │ Moderate │
Setup Time          │  < 1 min │  ~3 min  │
─────────────────────────────────────────────────────────

Use MLflow when:
• Working offline
• Need local-first solution
• Simple experiments
• Integration with existing MLflow infrastructure

Use W&B when:
• Need collaboration
• Want rich visualizations
• Real-time monitoring required
• Team-based workflows
```

---

## 🔑 Key Takeaways

1. **Abstraction Matters**: Makefile reduced command length by 82%
2. **Organization is UX**: Clean structure = better user experience
3. **Progressive Disclosure**: Simple entry → detailed guides
4. **Configuration Over Code**: YAML configs for easy customization
5. **Documentation Hierarchy**: README → Getting Started → Detailed Guides

---

**For more details, see:**
- [Main README](../README.md)
- [Getting Started Guide](../GETTING_STARTED.md)
- [Before & After Comparison](../BEFORE_AFTER.md)
